import { Transaction } from '@mysten/sui/transactions';

export const deposit = async ({
  tx = new Transaction(),
  // amount,
  // unspentUtxos = [],
  // vortexSdk,
  // vortexKeypair,
  // vortexPool,
  // root,
  // getMerklePathFn,
  // relayer,
  relayerFee = 0n,
}) => {};
